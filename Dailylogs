**Day 1 - 3.5.2026**

**Chapter 1 - Revision**
What I learned?
    -Practiced Commands
    -Revised Theory

Where I stuck?
    -whereis, which, locate commands
    -BinariesChapter 1 - Revision

**Chapter 2 - Revision**
What I learned?
    -Text Manipulation
    -Commands practice

Where I stuck
    -Search lines that start with root in /etc/passwd
    -Search lines ending with .conf
    -Watch new lines being added live to access.log

Solutions:
    - "\." is for actual . not character in regex
    - $ = End of Line (Meaning vary depending on situation)

**Chapter 3 - Revision**
What I Learned?
    -netstat - Shows all the connection coming and going out of the system
    -ss - Detailed and human readable

Where I stuck?
    -dns server

**Chapter 4 - Revision**
What I learned?
    -apt remove -> Does'nt remove configuration files
    -apt purge -> removes file completely

Where I stuck?
    -Nowhere !

**Chapter 5 - Revision**
What I learned?
        -Nothing new just revision.

**Chapter 6 - Revision**
What I learned?
    -nice - used to control process priority, nice is used to set priority before the process starts
            nice -n 10 command
    -renice - used to set priority after the process starts or already running process
            renice -n 10 -p 123
    NOTE: priority more than 0 or negative values requires root previlige

**Chapter 7 - Revision**
What I learned?
    $PATH are variables that system check before everthing and provides the output from that 
    if you want to run an executable file from anywhere in the system add it to the PATH variable
    and walla you can execute it from anywhere
    I personally created a script to update and upgrade the system and added it to the path


**Chapter 8 - Revision**
What I learned?
    can be use to run nmap scan (I build my own nmap scanner)

**Chapter 9 - Study**
    -tar = tape archive, used to combine multiple files together also known as archives
        -gzip(GNU zip) = .tar.gz or .tgz, decompress by gunzip
        -bzip2 = .tar.bz2, bunzip2 for decompressing
        -compress = .tar.z, uncompress for decompressing
    -dd - Linux archiving command stand on the top of the rest in its usefulness, used copy
          entire disk, hard drive, filesystem
        Deleted files wont be copy using most logical utility, cp

**Chapter 10 - Study**
    -dev: dev stands for device dev contains all the files representing attached device